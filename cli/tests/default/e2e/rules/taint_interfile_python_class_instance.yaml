rules:
  - id: taint_interfile_python_class_instance
    message: Taint should flow through imported Python class instances
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
