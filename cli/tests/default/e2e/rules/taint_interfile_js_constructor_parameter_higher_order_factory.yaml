rules:
  - id: taint_interfile_js_constructor_parameter_higher_order_factory
    message: Taint should flow through JavaScript constructor parameter higher-order factories
    languages: [js]
    severity: WARNING
    mode: taint
    options: { interfile: true }
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
