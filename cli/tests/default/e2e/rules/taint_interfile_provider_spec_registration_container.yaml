rules:
  - id: taint_interfile_provider_spec_registration_container
    message: Taint should flow through JavaScript provider spec registration calls
    languages: [js]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
