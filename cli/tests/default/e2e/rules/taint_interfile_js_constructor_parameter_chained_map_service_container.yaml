rules:
  - id: taint_interfile_js_constructor_parameter_chained_map_service_container
    message: Taint should flow through JavaScript chained map service containers
    languages: [js]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
