rules:
  - id: taint_interfile_typescript_registry_metadata
    message: Taint should flow through TypeScript registry decorator metadata
    languages: [typescript]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
