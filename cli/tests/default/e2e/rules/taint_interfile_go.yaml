rules:
  - id: taint_interfile_go
    message: Taint should flow through Go package functions in other files
    languages:
      - go
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
