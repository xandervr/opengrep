rules:
  - id: taint_interfile_python
    message: Taint should flow through Python imports in other files
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
