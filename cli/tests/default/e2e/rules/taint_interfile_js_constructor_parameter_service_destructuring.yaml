rules:
  - id: taint_interfile_js_constructor_parameter_service_destructuring
    message: Taint should flow through JavaScript destructured service containers
    languages: [js]
    severity: WARNING
    mode: taint
    options: { interfile: true }
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
