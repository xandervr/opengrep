rules:
  - id: taint_interfile_python_wildcard_import
    message: Taint should flow through Python wildcard imports
    languages:
      - python
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
