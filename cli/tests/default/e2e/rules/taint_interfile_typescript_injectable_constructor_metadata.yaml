rules:
  - id: taint_interfile_typescript_injectable_constructor_metadata
    message: Taint should flow through TypeScript injectable constructor metadata
    languages: [typescript]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
