rules:
  - id: taint_interfile_js_constructor_parameter_service_container_factory
    message: Taint should flow through JavaScript service-container factory returns
    languages: [js]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
