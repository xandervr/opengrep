rules:
  - id: taint_interfile_typescript_provider_token_metadata
    message: Taint should flow through TypeScript provider token metadata
    languages: [typescript]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
