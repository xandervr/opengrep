rules:
  - id: taint_interfile_typescript_decorated_metadata_injection
    message: Taint should flow through TypeScript decorator metadata injection
    languages: [typescript]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
