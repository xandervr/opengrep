rules:
  - id: taint_interfile_java_unqualified_field
    message: Taint should flow through unqualified Java instance fields
    languages:
      - java
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
