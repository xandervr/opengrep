rules:
  - id: taint_interfile_typescript_parameter_property
    message: Taint should flow through TypeScript constructor parameter properties
    languages: [typescript]
    severity: WARNING
    mode: taint
    options: { interfile: true }
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
