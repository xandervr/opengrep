rules:
  - id: taint_interfile_provider_object_array_container
    message: Taint should flow through JavaScript provider object array containers
    languages: [js]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
