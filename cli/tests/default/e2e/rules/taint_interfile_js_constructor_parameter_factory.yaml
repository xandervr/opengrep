rules:
  - id: taint_interfile_js_constructor_parameter_factory
    message: Taint should flow through JavaScript constructor parameter factory aliases
    languages: [js]
    severity: WARNING
    mode: taint
    options: { interfile: true }
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
