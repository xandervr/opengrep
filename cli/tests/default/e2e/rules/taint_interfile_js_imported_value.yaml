rules:
  - id: taint_interfile_js_imported_value
    message: Taint should flow through imported JavaScript module values
    languages:
      - js
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
