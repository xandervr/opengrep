rules:
  - id: taint_interfile_typescript_provider_environment_metadata
    message: Taint should flow through TypeScript environment provider metadata
    languages: [typescript]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
