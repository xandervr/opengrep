rules:
  - id: taint_interfile_typescript_provider_metadata_spread_container
    message: Taint should flow through TypeScript provider metadata spreads
    languages: [typescript]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
