rules:
  - id: taint_interfile_js_constructor_parameter_service_container_factory_alias
    message: Taint should flow through JavaScript service-container factory aliases
    languages: [js]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
