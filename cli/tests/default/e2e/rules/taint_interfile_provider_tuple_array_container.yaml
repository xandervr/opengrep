rules:
  - id: taint_interfile_provider_tuple_array_container
    message: Taint should flow through JavaScript provider tuple arrays
    languages: [js]
    severity: WARNING
    mode: taint
    options:
      interfile: true
    pattern-sources:
      - pattern: source(...)
    pattern-sinks:
      - pattern: sink(...)
